# Phase 2: Core UI Components

1. Build the `Header` component featuring date navigation logic with `< [Day, Month Date] >` format.
2. Create the `FAB` (Floating Action Button) with a glowing SVG icon inspired by the Lucide-react aesthetic.
3. Implement a custom `BottomSheet` drawer for entry creation and editing, ensuring smooth transitions and mobile-first ergonomics.
4. Develop the `EntryCard` component variants for Note, Task, and Event types with their respective icons and subtle glows.
5. Standardize input controls within the drawer, including a text box, radio buttons for categories, and "Void/Enter" actions.
