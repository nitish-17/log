# Phase 4: Entry Interactions & Editing

1. Link the FAB to trigger the `BottomSheet` in "Creation Mode" for new log entries.
2. Implement long-press interaction on `EntryCard` components to open the `BottomSheet` in "Editing Mode" pre-filled with data.
3. Integrate the "Delete" icon within the `BottomSheet` drawer, ensuring it only appears during editing sessions.
4. Connect the UI state of the drawer to the Dexie.js database operations for real-time persistence.
5. Add haptic feedback or visual cues for long-press and successful "Enter" (commit) actions to enhance the tactile feel.
